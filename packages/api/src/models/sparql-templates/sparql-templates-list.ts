import {SparqlTemplate} from './sparql-template';

// TODO: Reimplement this as a proper ModelList class later when services are migrated too.
export interface SparqlTemplatesList {
  items: SparqlTemplate[];
}
