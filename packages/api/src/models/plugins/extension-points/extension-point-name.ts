/**
 * Enum representing the names of all available extension points in the system.
 */
export enum ExtensionPointName {
  MAIN_MENU = 'main.menu',
  ROUTE = 'route',
  INTERACTIVE_GUIDE = 'guide.step',
  THEMES = 'themes'
}
